environment:
  SPARK_TOKEN: 
  SPARK_ROOM:
  DOCKER_USERNAME:
  DOCKER_PASSWORD:
  DOCKER_EMAIL:
  MANTL_USERNAME:
  MANTL_PASSWORD:
  # Provide the DNS or IP of the Mantl Control Node WITHOUT the https:// prefix
  MANTL_CONTROL:
  DEPLOYMENT_DIR:
  BOT_NAME:
